<UserControl x:Class="SilkySouls2.Views.Tabs.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             FocusManager.FocusedElement="{x:Null}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <DataTemplate x:Key="HotkeyItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="{Binding DisplayName}"
                           Width="130"
                           VerticalAlignment="Center" />
                <TextBox Text="{Binding HotkeyText}"

                         Style="{StaticResource HotkeyTextBoxStyle}"
                         Tag="{Binding ActionId}"
                         GotFocus="HotkeyTextBox_GotFocus"
                         LostFocus="HotkeyTextBox_LostFocus"
                         KeyDown="HotkeyTextBox_KeyDown" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    
    
    <Border BorderThickness="1" BorderBrush="#333" CornerRadius="3" Margin="5">
        <StackPanel Margin="10">
            <TextBlock Text="Options" FontWeight="Bold" Margin="0,0,0,10" />

            <Grid Margin="0,5,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                </Grid.ColumnDefinitions>
                
                <CheckBox Grid.Row="0" Grid.Column="0" Content="Faster Menu (Skips dialogs and online check)"
                          IsChecked="{Binding IsFastQuitoutEnabled}"
                          Margin="0,0,10,0" />
                <CheckBox Grid.Row="1" Grid.Column="0" Content="Always On Top"
                          IsChecked="{Binding IsAlwaysOnTopEnabled}"
                          Margin="0,5,0,0" />
                
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal"
                          Margin="0,5,0,0">
                <CheckBox Content="Baby jump fix"
                          IsChecked="{Binding IsBabyJumpFixEnabled}"
                          Margin="0,5,0,0" />
                <CheckBox Content="Disable Double Click"
                          IsChecked="{Binding IsDisableDoubleClickEnabled}"
                          Margin="10,5,0,0" />
                </StackPanel>
            </Grid>

            <Separator Margin="0,0,0,10" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Hotkey Bindings" FontWeight="Bold" Margin="10,0,0,10" HorizontalAlignment="Left" />
                <CheckBox Content="Enable Hotkeys"
                          IsChecked="{Binding IsEnableHotkeysEnabled}"
                          Margin="40,0,0,10" />
                <CheckBox Content="Block Game Actions"
                          IsChecked="{Binding IsBlockGameHotkeysEnabled}"
                          Margin="20,0,10,10" />

                <Border Background="#3498db" CornerRadius="10" Width="12" Height="12" Margin="5,0,0,10"
                        VerticalAlignment="Center"
                        ToolTip="Disables game actions from happening when you press a hotkey that you've bound to something in here.&#x0A;This may cause some lag depending on your setup"
                        ToolTipService.InitialShowDelay="100">
                    <TextBlock Text="i" Foreground="White" HorizontalAlignment="Center"
                               VerticalAlignment="Center" FontWeight="Bold" FontSize="10" />
                </Border>
                <Button
                    Content="Clear All Bindings"
                    Margin="15,0,0,10"
                    Command="{Binding ClearHotkeysCommand}" />
            </StackPanel>
            
            <TabControl x:Name="HotkeyCategoriesTabControl" Margin="0,5,0,0">
                <TabItem Header="Player">
                    <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding PlayerHotkeys}"
                                      Padding="5"
                                      ItemTemplate="{StaticResource HotkeyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Enemies">
                    <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding EnemiesHotkeys}"
                                      Padding="5"
                                      ItemTemplate="{StaticResource HotkeyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Target">
                    <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding TargetHotkeys}"
                                      Padding="5"
                                      ItemTemplate="{StaticResource HotkeyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Utility">
                    <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding UtilityHotkeys}"
                                      Padding="5"
                                      ItemTemplate="{StaticResource HotkeyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Border>
</UserControl>