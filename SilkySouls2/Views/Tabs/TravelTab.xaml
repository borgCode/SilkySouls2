<UserControl x:Class="SilkySouls2.Views.Tabs.TravelTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:SilkySouls2.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="NullToVisibilityConverter" />

        <!-- Shared list item containers -->
        <Style x:Key="GroupListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" Background="Transparent" Padding="8,4">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WarpListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="true">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="SearchBoxTemplate" TargetType="TextBox">
            <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3">
                <Grid>
                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden"
                                  VerticalAlignment="Center" Margin="5,0,0,0" />
                    <TextBlock Text="Search locations..." VerticalAlignment="Center"
                               HorizontalAlignment="Left" Margin="5,0,0,0"
                               Foreground="Gray" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="" />
                                            <Condition Binding="{Binding Path=IsKeyboardFocused, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter TargetName="border" Property="BorderThickness" Value="1.5" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <DataTemplate x:Key="WarpItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Text="{Binding Area}"
                           Foreground="#888888"
                           FontStyle="Italic"
                           Margin="10,0,0,0" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <!-- AREAS -->
            <TabItem Header="Areas">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="190" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Grid.RowSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Areas" Margin="0,0,0,5" />
                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding Areas.Groups}"
                                 SelectedItem="{Binding Areas.SelectedGroup}"
                                 BorderThickness="1" BorderBrush="#333"
                                 ItemContainerStyle="{StaticResource GroupListBoxItemStyle}" />
                        <Button Grid.Row="2" Content="Warp"
                                Command="{Binding AreaWarpCommand}"
                                IsEnabled="{Binding AreOptionsEnabled}"
                                Height="25" Margin="0,10,0,0" />
                    </Grid>

                    <TextBox Grid.Row="0" Grid.Column="1"
                             Text="{Binding Areas.SearchText, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalAlignment="Right" Width="150" Height="25"
                             VerticalAlignment="Center" Margin="0,0,0,5"
                             Template="{StaticResource SearchBoxTemplate}" />

                    <Border Grid.Row="1" Grid.Column="1" Margin="10,0,0,0"
                            BorderThickness="1" BorderBrush="#333">
                        <ListView ItemsSource="{Binding Areas.Items}"
                                  SelectedItem="{Binding Areas.SelectedItem}"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  BorderThickness="0"
                                  ItemContainerStyle="{StaticResource WarpListViewItemStyle}"
                                  ItemTemplate="{StaticResource WarpItemTemplate}" />
                    </Border>
                </Grid>
            </TabItem>

            <!-- BOSSES -->
            <TabItem Header="Bosses">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="190" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Grid.RowSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Areas" Margin="0,0,0,5" />
                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding Bosses.Groups}"
                                 SelectedItem="{Binding Bosses.SelectedGroup}"
                                 BorderThickness="1" BorderBrush="#333"
                                 ItemContainerStyle="{StaticResource GroupListBoxItemStyle}" />
                        <Button Grid.Row="2" Content="Warp"
                                Command="{Binding BossWarpCommand}"
                                IsEnabled="{Binding AreOptionsEnabled}"
                                Height="25" Margin="0,10,0,0" />
                    </Grid>

                    <TextBox Grid.Row="0" Grid.Column="1"
                             Text="{Binding Bosses.SearchText, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalAlignment="Right" Width="150" Height="25"
                             VerticalAlignment="Center" Margin="0,0,0,5"
                             Template="{StaticResource SearchBoxTemplate}" />

                    <Border Grid.Row="1" Grid.Column="1" Margin="10,0,0,0"
                            BorderThickness="1" BorderBrush="#333">
                        <ListView ItemsSource="{Binding Bosses.Items}"
                                  SelectedItem="{Binding Bosses.SelectedItem}"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  BorderThickness="0"
                                  ItemContainerStyle="{StaticResource WarpListViewItemStyle}"
                                  ItemTemplate="{StaticResource WarpItemTemplate}" />
                    </Border>
                </Grid>
            </TabItem>

            <!-- CUSTOM -->
            <TabItem Header="Custom">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="190" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Grid.RowSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Categories" Margin="0,0,0,5" />
                        <ListBox Grid.Row="1"
                                 ItemsSource="{Binding CustomWarps.Groups}"
                                 SelectedItem="{Binding CustomWarps.SelectedGroup}"
                                 BorderThickness="1" BorderBrush="#333"
                                 ItemContainerStyle="{StaticResource GroupListBoxItemStyle}" />
                        <Button Grid.Row="2" Content="Warp"
                                Command="{Binding CustomWarpCommand}"
                                IsEnabled="{Binding AreOptionsEnabled}"
                                Height="25" Margin="0,10,0,0" />
                    </Grid>

                    <TextBox Grid.Row="0" Grid.Column="1"
                             Text="{Binding CustomWarps.SearchText, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalAlignment="Right" Width="150" Height="25"
                             VerticalAlignment="Center" Margin="0,0,0,5"
                             Template="{StaticResource SearchBoxTemplate}" />

                    <Border Grid.Row="1" Grid.Column="1" Margin="10,0,0,0"
                            BorderThickness="1" BorderBrush="#333">
                        <ListView ItemsSource="{Binding CustomWarps.Items}"
                                  SelectedItem="{Binding CustomWarps.SelectedItem}"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  BorderThickness="0"
                                  ItemContainerStyle="{StaticResource WarpListViewItemStyle}"
                                  ItemTemplate="{StaticResource WarpItemTemplate}" />
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="1"
                BorderThickness="1" BorderBrush="#333" CornerRadius="3"
                Margin="0,10,0,0">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Unlock All Bonfires"
                        Command="{Binding UnlockBonfiresCommand}"
                        IsEnabled="{Binding AreOptionsEnabled}"
                        Padding="10,2" />
                <Button Grid.Column="1" Content="Create Custom Warp"
                        Command="{Binding OpenCreateCustomWarpCommand}"
                        Padding="10,2"
                        Margin="10,0,0,0" />
                <CheckBox Grid.Column="3" Content="Rest on Warp"
                          IsChecked="{Binding IsRestOnWarpEnabled}"
                          VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
